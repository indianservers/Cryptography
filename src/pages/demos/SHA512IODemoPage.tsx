import { HashIODemoPage } from "./HashIODemoPage";

export default function SHA512IODemoPage() {
  return <HashIODemoPage title="SHA-512 Input / Output" algorithm="SHA-512" status="Modern" note="SHA-512 is a modern hash. It is one-way and has no decryption operation." />;
}
