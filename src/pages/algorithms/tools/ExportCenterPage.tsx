import { AlgorithmPageShell } from "../../../components/common/AlgorithmPageShell";

export default function ExportCenterPage() {
  return <AlgorithmPageShell title="Export Center" category="Export Center" status="Educational" intro="Conceptual export preview for learning artifacts. Redaction is the default expectation: exported summaries should include route, title, status, exactness, safety notes, timestamp, and whether user input was redacted." inputs={["Export type","Scope","Filename","Redact inputs by default"]} outputs={["Export metadata summary","Redacted payload preview","Safety and exactness notes"]} visualizers={["Export format tabs","Saved experiment selector","Redaction review checklist"]} notes={["Review exports before sharing because they may contain keys, plaintext, passwords, tokens, or other sensitive data.","Conceptual or verification-pending modules must include that exactness note in exported metadata.","Deprecated or unsafe algorithms must include the safety warning in exported metadata."]} />;
}
